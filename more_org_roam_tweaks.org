#+title: More org-roam tweaks
#+roam_alias: "emacs channel" "emacs-elements"
#+created: [2021-02-11 Thu]
#+last_modified: [2021-02-11 Thu 09:30]
#+ROAM_TAGS: org-roam




*open-recent-files*




(winner-mode +1)

(define-key winner-mode-map (kbd "<M-left>") #'winner-undo)

(define-key winner-mode-map (kbd "<M-right>") #'winner-redo)

This tweak allows you to revert actions done in the org-roam window. I will cover this in more detail in another video.

*DEFT: find files by content*

#+begin_src emacs-lisp
(require 'deft)
(setq deft-directory "~/org-roam"
      deft-extensions '("md" "org"))
(setq deft-recursive t)
(setq deft-use-filename-as-title nil)
(setq deft-use-filter-string-for-filename t)
(setq deft-file-naming-rules '((nospace . "-")))
#+end_src

*ORG_DOWNLOAD*

#+DOWNLOADED: file:///home/red/Desktop/flower-wallpaper-20.jpg @ 2021-02-11 12:05:47
[[file:2021-02-11_12-05-47_flower-wallpaper-20.jpg]]

Insert pictures into org-roam files

#+begin_src emacs-lisp
(require 'org-download)
;; Drag-and-drop to `dired`
(add-hook 'dired-mode-hook 'org-download-enable)
(setq org-download-image-dir "~/Pictures")
(setq org-download-heading-lvl nil)
(setq org-download-timestamp "%Y%m%d-%H%M%S_")
(setq org-image-actual-width 300)
#+end_src

*alternative titles* org
#+roam_alias:

e.g. Lent 1
First Sunday in Lent

org-roam-title-sources

*org-roam keys*

#+roam_key: 

Google
https://www.google.com/
